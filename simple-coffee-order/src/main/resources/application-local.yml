spring:
  datasource:
    url: jdbc:mysql://localhost:3306/simple-coffee-order?serverTimezone=UTC
    username: root
    password: root
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create